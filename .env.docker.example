# Docker deployment environment
# On the server, copy this file to .env and fill in your values.
# docker-compose reads this file and passes the vars as build args.
NEXT_PUBLIC_API_URL=https://api.yourdomain.com/
