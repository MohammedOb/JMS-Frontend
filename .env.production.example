# Production frontend environment
# Point this to your deployed backend API domain.
NEXT_PUBLIC_API_URL=https://api.yourdomain.com/

# Production does not use Next.js dev origins, so leave this unset.
